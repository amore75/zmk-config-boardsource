# build.yaml

# This file defines how to build ZMK firmware for your custom board + shield.
# For SuperMini NRF52840, use the Seeeduino XIAO NRF52840 board target.

include:
  - board: seeeduino_xiao_nrf52840
    shield: boardsource_3x4
